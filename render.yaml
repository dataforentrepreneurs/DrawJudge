services:
  # Main Backend Service (serving API + Frontends)
  - type: web
    name: party-games-hub
    env: python
    plan: free
    buildCommand: |
      # Install python dependencies
      pip install -r Server/requirements.txt
      
      # Build Launcher (Only if folder exists)
      if [ -d "Games/Launcher" ]; then
        cd Games/Launcher && npm install && npm run build && cd ../..
      fi
      
      # Build DrawJudge (Only if folder exists)
      if [ -d "Games/DrawJudge" ]; then
        cd Games/DrawJudge && npm install && npm run build && cd ../..
      fi
    startCommand: |
      cd Server
      python -m uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: party-games-redis
          property: connectionString
      - key: SENTRY_DSN
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: VITE_POSTHOG_KEY
        sync: false
      - key: VITE_POSTHOG_HOST
        value: https://us.i.posthog.com
      - key: ENVIRONMENT
        value: production
      - key: LAUNCHER_DIST
        value: /opt/render/project/src/Games/Launcher/dist
      - key: DRAWJUDGE_DIST
        value: /opt/render/project/src/Games/DrawJudge/dist

  # Redis instance for persistence
  - type: keyvalue
    name: party-games-redis
    plan: free
    ipAllowList: [] 
